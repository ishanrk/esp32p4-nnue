{
  "name": "esp32p4-nnue-guide",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run sync && vite",
    "sync": "node scripts/sync-data.mjs",
    "check": "npm run sync && tsc --noEmit && tsx src/site.test.ts",
    "build": "npm run sync && vite build && node scripts/postbuild.mjs && node scripts/validate.mjs"
  },
  "dependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/node": "24.12.0",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@vitejs/plugin-react": "6.1.0",
    "tsx": "4.23.12",
    "typescript": "7.0.2",
    "vite": "8.2.2"
  }
}
